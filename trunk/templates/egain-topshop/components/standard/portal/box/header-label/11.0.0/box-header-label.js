define(['core/component'], function(BaseComponent){

    var BoxTopNavComponent = BaseComponent.extend({

        name : "box-support",

        prepare : function() {

            this.render();
        }
    });

    return BoxTopNavComponent;
});
